spring:
  datasource:
    url: jdbc:mysql://localhost:3306/maps_conect_dev
    username: root
    password: Haloymega59!
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        generate_statistics: false

logging:
  level:
    root: INFO
    com.tecmilenio.mapsconect: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
